// PreferenceChip component — owned by Mobile Agent (implementation)
// Selectable chip button for preference selection during onboarding

import { View, Text } from 'react-native';

// TODO: Implement selectable preference button
export default function PreferenceChip() {
  return (
    <View>
      <Text>PreferenceChip</Text>
    </View>
  );
}
